import styled from 'styled-components';

const HeaderHolder = styled.div`
    display: flex;
    justify-content: center;
    padding: 0.7rem;
    background-color: rgba(0, 45, 55, 0.7);
`;

export default HeaderHolder;