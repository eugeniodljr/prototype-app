import styled from "styled-components";
import { Link } from "react-router-dom";

const SearchLinkHolder = styled(Link)`
    text-decoration: none;
    color: antiquewhite;
`;

export default SearchLinkHolder;